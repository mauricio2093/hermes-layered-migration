"""Layer 2: our own code on top of upstream Hermes.

Nothing here modifies an upstream file. The package can be deleted and Hermes
keeps working; that is the property that makes upstream re-clonable.
"""
