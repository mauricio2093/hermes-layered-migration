# This repository holds tooling and doctrine. It never holds state or secrets.
.env
*.db
*.db-wal
*.db-shm
auth.json
backups/
rescue/
__pycache__/
*.py[cod]
.venv/
venv/

# Rust build output. Cargo.lock IS committed: an unattended maintenance
# binary should build from the same dependency versions every time.
rust/target/
